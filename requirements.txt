# ExamManager — runtime dependencies.
# Python 3.11 – 3.14 (minimum 3.11). Every pin below installs from a
# pre-built wheel on Windows, macOS and Linux for 3.11 through 3.14
# (odfpy is pure Python). Install with:  pip install -r requirements.txt
Flask==3.1.2
Werkzeug==3.1.4
waitress==3.0.2
python-dotenv==1.2.1
google-genai==1.60.0
numpy==2.3.5
opencv-python-headless==4.13.0.92
pillow==12.0.0
PyMuPDF==1.26.5
openpyxl==3.1.5
odfpy==1.4.1
